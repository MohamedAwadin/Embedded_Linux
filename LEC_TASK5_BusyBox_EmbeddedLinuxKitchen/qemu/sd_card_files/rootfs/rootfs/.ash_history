mkdir boot
ls
mount -t devtmpfs dtfs /dev/
mount -t vfat /dev/mmcblk0p1 /boot/
ls boot/
cd boot/extlinux/
cat extlinux.conf 
cd boot/
ls
cd /dev/
ls
cd 
exec /bin/sh
mknod /dev/console c 5 1
exec /bin/sh
cat /proc/cmdline
mount -o remount,rw /
mount -t devtmpfs dev /dev
ls -l /dev/console /dev/tty*
setsid cttyhack sh
ls -l /dev/console /dev/ttyAMA0
